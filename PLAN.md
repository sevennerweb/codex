# 모바일 여행 플래너 웹앱 구축 계획

## 요약

- 신규 프로젝트로 모바일 우선 반응형 웹앱을 구축한다.
- 로컬 Windows PC에서 실행하고 Cloudflare Tunnel로 고정 HTTPS 주소를 제공한다. PC가 켜져 있을 때만 접속 가능하다.
- 본인과 초대받은 동행자만 여행을 조회·편집한다.
- 1차 버전은 항공편 비교·선택, 일정 관리, 장소 검색, YouTube 재생, 날씨, 신칸센 예약 정보 기록까지 지원하며 결제는 공식 외부 사이트에서 진행한다.

## 디자인 방향과 UI 시스템

- 전체 콘셉트는 일반적인 관리자 대시보드가 아닌 `디지털 여행 노트 + 날짜별 일정 타임라인`으로 정한다. 정보량이 많아도 차분하고 가볍게 읽히는 모던한 여행 서비스 스타일을 유지한다.
- 밝은 아이보리 계열 배경, 딥 네이비 텍스트, 청록색 주요 액션을 기본 팔레트로 사용하고 항공·숙소·관광·교통·날씨는 절제된 보조색과 아이콘으로 구분한다. 색상만으로 상태를 전달하지 않고 텍스트나 아이콘을 함께 표시한다.
- 서체 크기, 간격, 모서리, 그림자, 색상은 디자인 토큰으로 관리한다. 카드는 둥근 모서리와 옅은 테두리 또는 최소한의 그림자를 사용하고, 충분한 여백과 명확한 제목 계층으로 정보를 나눈다.
- 모바일에서는 상단 여행 요약과 하단 고정 내비게이션을 사용하고, 핵심 액션은 한 손으로 누르기 쉬운 위치와 최소 44px 터치 영역을 확보한다. 날짜별 일정은 세로 타임라인과 접이식 섹션으로 표현한다.
- 태블릿과 데스크톱에서는 좌측 여행 내비게이션과 넓은 콘텐츠 영역을 사용한다. 일정 화면은 날짜 목록과 선택 날짜의 상세 일정을 함께 볼 수 있도록 확장하되 모바일과 동일한 정보 구조를 유지한다.
- 확정된 날짜·공항·항공편은 잠금 아이콘과 상태 배지로 구분하고, 수정 모드 진입 시 해당 영역만 명확히 활성화한다. 저장·확정처럼 영향이 큰 액션은 결과 피드백과 되돌아갈 경로를 제공한다.
- 드래그 가능한 일정 카드는 드래그 핸들, 키보드 이동 대안, 이동 중 위치 표시를 제공한다. 펼치기·접기와 화면 전환 애니메이션은 짧게 사용하고 `prefers-reduced-motion` 환경에서는 제거한다.
- 모든 화면에 로딩 스켈레톤, 빈 상태 안내와 다음 행동, 입력 오류, 외부 API 실패, 저장 성공 피드백을 동일한 컴포넌트 규칙으로 제공한다.
- 입력 요소는 항상 보이는 레이블과 도움말을 갖고, 키보드 탐색 순서, 포커스 표시, 본문·버튼 명암 대비를 접근성 기준에 맞춘다.
- 반응형 완성 기준은 약 375px 모바일, 768px 태블릿, 1280px 이상 데스크톱에서 가로 스크롤이나 잘림 없이 핵심 작업을 완료할 수 있는 상태로 정한다.

## 주요 구현

- TypeScript 기반 풀스택 웹앱과 SQLite DB로 구성하고 Docker Compose로 앱과 Cloudflare Tunnel을 실행한다.
- 모바일 화면은 `여행 기본정보 → 항공편 → 일자별 일정 → 라이브 영상 → 교통` 순서의 카드/탭 구조로 만든다.
- 관리자 초대 방식의 계정을 사용한다. 관리자가 사용자명과 임시 비밀번호를 발급하고 첫 로그인 때 비밀번호를 변경한다.
- 여행별 `관리자/편집자` 권한을 두고, 서버에서 모든 수정 권한을 검증한다.
- 날짜와 출국·귀국 구간은 각각 `작성 중/확정` 상태를 갖는다. 확정 후에는 잠기며 명시적으로 수정 모드로 전환해야 변경할 수 있다.
- 왕복과 오픈조를 지원한다. 가는 편과 오는 편의 출발·도착 공항을 독립적으로 검색하고 IATA 코드로 저장한다.
- 초기 임시 공항 목록은 전 세계 상용 공항을 포함하는 IATA 공항 데이터셋으로 교체한다. 공항명, 도시명, IATA 코드, 현지어·영문·한국어 별칭으로 검색할 수 있어야 하며 `시즈오카`, `후지산`, `FSZ`처럼 서로 다른 검색어가 동일한 공항을 찾도록 한다.
- 공항 데이터는 앱 내부의 버전이 지정된 정적 데이터로 제공해 외부 API 장애 중에도 검색 가능하게 하고, 데이터 출처·라이선스·마지막 갱신일을 기록한다. 중복 코드, 폐항, IATA 코드가 없는 시설은 빌드 시 검증하거나 검색 대상에서 제외한다.
- Amadeus Flight Offers API를 서버에서 호출해 총액 기준 오름차순으로 표시하고 `직항만/경유 포함` 필터를 제공한다. 선택한 항공편은 가격, 운항사, 편명, 시간, 경유 정보를 스냅샷으로 저장한다.
- 항공권 결제는 앱 밖에서 진행한다. 제공 가능한 예약 링크가 없으면 항공사 또는 검색 사이트로 이동한 후 예약 결과를 앱에 수동 확정한다. API 운임은 판매처 가격과 다를 수 있음을 표시한다. [Amadeus 공식 안내](https://admin.developers.amadeus.com/self-service/apis-docs/guides/developer-guides/faq/)
- 여행 기간의 날짜별 섹션을 자동 생성한다. 섹션은 접기/펼치기가 가능하고 일정 카드는 같은 날짜 안에서 정렬하거나 다른 날짜로 드래그할 수 있다.
- 일정 카드는 시간, 유형, 장소명, 주소, 좌표, 메모, 호텔 체크인·체크아웃 정보를 지원한다.
- 장소 검색은 OpenStreetMap 기반 검색을 서버 경유로 제공한다. 검색 버튼 방식, 요청 제한, 결과 캐시, 출처 표기를 적용하고 과도한 자동완성 호출은 하지 않는다.
- Open-Meteo로 일정 장소의 일별 최저·최고기온, 강수 확률, 날씨 상태를 표시한다. 최대 16일 예보 범위 밖에는 `아직 예보 없음`을 표시한다. [Open-Meteo 공식 문서](https://open-meteo.com/en/docs)
- 사용자가 YouTube URL을 추가하면 영상 ID를 검증해 반응형 플레이어로 재생한다. 임베딩이 금지되었거나 삭제된 영상은 원본 링크를 제공한다. [YouTube IFrame API](https://developers.google.com/youtube/iframe_api_reference)
- 신칸센 검색 조건과 예약 결과를 기록하는 화면을 제공한다. SmartEX 공식 사이트로 이동해 좌석을 확인하고 열차명·번호, 구간, 시간, 좌석 등급, 차량·좌석, 예약번호를 앱에 저장한 뒤 일정 카드로 추가한다. [SmartEX 좌석 확인 절차](https://smart-ex.jp/en/reservation/reserve_smart/sp/)
- 동시 실시간 편집은 제외한다. 각 데이터에 버전을 두어 오래된 화면에서 저장하면 덮어쓰지 않고 새로고침 안내를 표시한다.
- 관리자 전용 JSON 내보내기와 검증된 JSON 복원 기능을 제공한다.
- API 키, DB, 비밀번호 해시와 터널 자격 증명은 브라우저에 노출하지 않는다. 로그인 쿠키는 HTTPS·HttpOnly·SameSite 설정을 적용한다.

## 주요 데이터 구조와 인터페이스

- `User`, `Trip`, `TripMember`: 사용자, 여행, 여행별 권한.
- `TravelLeg`: 출국·귀국 구간, 공항, 날짜, 확정 상태.
- `FlightSearchSnapshot`, `SelectedFlight`: 검색 조건과 확정 항공편 스냅샷.
- `TripDay`, `ScheduleItem`: 날짜와 정렬 가능한 일정 카드.
- `SavedVideo`: YouTube URL, 영상 ID, 제목, 연결 장소.
- `ShinkansenPlan`: 검색 조건과 수동 확정한 열차·좌석 정보.
- 외부 연동은 항공, 장소, 날씨 제공자별 서버 어댑터로 분리해 향후 API 교체가 가능하게 한다.

## 테스트 및 완료 기준

- 모바일 Chrome/Safari에서 로그인, 여행 생성, 날짜·오픈조 구간 입력과 확정 잠금이 정상 동작한다.
- 공항 검색, 직항 필터, 가격 정렬, 항공편 선택과 API 오류 안내를 검증한다.
- 한국어·영문 공항명, 도시명, IATA 코드와 별칭 검색을 검증하고 시즈오카 공항(`FSZ`)을 회귀 테스트에 포함한다.
- 일정 추가·수정·삭제, 날짜 내 정렬, 날짜 간 드래그, 접기·펼치기와 새로고침 후 순서 보존을 검증한다.
- 두 사용자가 오래된 화면에서 같은 항목을 수정할 때 충돌 안내가 나오고 기존 데이터가 조용히 덮어써지지 않는다.
- 장소 검색 제한·캐시·출처 표시와 날씨 예보 범위 안팎의 상태를 검증한다.
- 일반 영상, 라이브 영상, 잘못된 URL, 임베딩 제한 영상의 동작을 검증한다.
- SmartEX 이동, 신칸센 정보 저장, 일정 전환을 검증한다.
- 권한 없는 여행 접근, 로그인 시도 제한, 비밀키 노출 여부와 JSON 내보내기·복원을 검증한다.
- 외부 모바일 네트워크에서 고정 HTTPS 주소로 접속하고, PC 또는 터널이 꺼졌을 때 예상대로 접속 불가가 되는지 확인한다.

## 가정과 제외 범위

- UI 기본 언어는 한국어, 가격 기본 통화는 KRW, 성인 여행자 수 기본값은 1명으로 하되 검색 전에 변경할 수 있게 한다.
- 장소와 날씨는 전 세계를 지원하고 신칸센 보조 기능은 SmartEX 지원 노선을 기준으로 한다.
- 항공권·신칸센의 앱 내부 구매, 결제, 취소와 자동 좌석 수집은 1차 버전에서 제외한다.
- 실시간 공동 편집, 채팅, 알림, 오프라인 편집, 자동 클라우드 백업은 제외한다.
- 도메인과 Cloudflare 계정, Amadeus API 키는 사용자가 준비한다.
- 현재 작업 폴더에는 기존 애플리케이션 코드가 없으므로 신규 프로젝트로 시작한다.
